enum Errors {
    notFound = "404",
    userNotFound = "User not found",
    authorNotFound = "Author not found",
    wrongCredentials = "Wrong credentials",
    credentialsAlreadyTaken = "Credentials already taken",
}